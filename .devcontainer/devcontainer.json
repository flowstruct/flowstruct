{
  "name": "flowstruct-app",
  "image": "node:22-alpine",
  "customizations": {
    "vscode": {
      "extensions": [
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint"
      ]
    }
  },
  "workspaceFolder": "/workspaces/flowstruct/flowstruct-app",
  "postCreateCommand": "apk add --no-cache git bash curl && npm install -g pnpm && pnpm install"
}